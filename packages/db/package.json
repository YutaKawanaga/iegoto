{
  "name": "@iegoto/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "dependencies": {
    "@iegoto/domain": "workspace:*",
    "@prisma/client": "^6.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "prisma": "^6.16.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
